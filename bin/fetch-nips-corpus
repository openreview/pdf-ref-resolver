#!/bin/bash

domain='https://papers.nips.cc'

## Fetch list of urls for each year
curl --silent "$domain" | htmlq a -a href --base "$domain" | grep -E '/paper/[[:digit:]]{4}$' > conference-years.txt

# cat conference-years.txt | xargs -i curl --silent {} | htmlq a -a href --base "$domain" | grep Abstract >> abstract-pages.txt
# head -n 30 2017.abstracts | xargs -i curl --silent {} | htmlq a -a href --base https://papers.nips.cc | grep -E 'Paper.pdf$' > 2017.pdf
# wget --wait 3 --random-wait -x -nH -i 2014.pdf
